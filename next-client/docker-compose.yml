services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cataur-next-client
    ports:
      - "${APP_PORT:-3000}:3000"
    environment:
      - NODE_ENV=production
      - APP_PORT=${APP_PORT:-3000}
    restart: unless-stopped
    networks:
      - cataur-network
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:${APP_PORT:-3000}"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

networks:
  cataur-network:
    driver: bridge
